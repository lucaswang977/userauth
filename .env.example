# CREATE ROLE puser LOGIN password 'localdbpassword';
# CREATE DATABASE testdb1 ENCODING 'UTF8' OWNER puser TEMPLATE template0;

DATABASE_URL=postgres://

# JWT secret can be generated by: 
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_SECS=300
JWT_REFRESH_EXPIRES_SECS=3600

EMAIL_ACTIVATE_EXPIRES_SECS=300
EMAIL_SERVER=
EMAIL_FROM=

PASSWORD_RESET_CODE_EXPIRES_SECS=300
